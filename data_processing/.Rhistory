setwd("~/OneDrive/Skrivebord/Sjette semester/Spatial Analytics/exam/accessibleaarhus/data_processing")
cph_map<-readr::read_rds("cph_map_data.rds")
århus_map<-readr::read_rds("aarhus_map_data.rds")
cat_aar <- unique(århus_map$amenity)
cat_aar <- sorted(unique(århus_map$amenity))
cat_aar <- sort(unique(århus_map$amenity))
cat_cph <- sort(unique(cph_map$amenity))
cph_map<- cph_map %>%
mutate(label_da = case_when(
wheelchair == "yes" ~ "Tilgængelig",
wheelchair == "limited" ~ "Begrænset tilgængelighed",
wheelchair == "no" ~ "Utilgængelig"
))
library(tidyverse)
cph_map<- cph_map %>%
mutate(label_da = case_when(
wheelchair == "yes" ~ "Tilgængelig",
wheelchair == "limited" ~ "Begrænset tilgængelighed",
wheelchair == "no" ~ "Utilgængelig"
))
View(cph_map)
View(cph_map)
View(cph_map)
cph_map
